
@using testdatamodel.Models
@{
    ViewBag.Title = "ShowAllQA";
    Layout = "~/Areas/Back/Views/Shared/_Layout.cshtml";
}


@helper ArticleResult(BackQA backQa)
{
    var artId = backQa.ID;
    var url = Url.Action("DetailQA", "BackMember") + "?qaId=" + artId;
    var title = backQa.Title;
    var main = backQa.Answer;
    <div class="card">
        <div class="card-header">
            <h5>@title</h5>
        </div>
        <div class="card-block">
            <form>
                @*<div class="form-group row">
                        <label class="col-sm-2 col-form-label">標題</label>
                        <div class="col-sm-10">
                            <input type="text" class="form-control" placeholder="" readonly>
                        </div>
                    </div>*@

                <div class="form-group row">
                    <a href="@url" class="col-sm-2 col-form-label">展開閱讀</a>
                </div>


            </form>

        </div>
    </div>
}


<h2>ShowAllQA</h2>
<p>
    @Html.ActionLink("新增QA", "AddQA")
</p>
<ul>
    @foreach (var qa in Model)
    {
        <li>@ArticleResult(qa)</li>
    }
</ul>